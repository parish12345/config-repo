eureka:
  instance:
    hostname: localhost
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka
spring:
  cloud:
    config:
      overrideSystemProperties: false
management:
  endpoints:
    web:
      exposure:
        include: '*'
