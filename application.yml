eureka:
  instance:
    hostname: ${EUREKA_HOSTNAME}
    client:
      serviceUrl:
        defaultZone: http://${EUREKA_HOSTNAME}:8761/eureka
spring:
  cloud:
    config:
      overrideSystemProperties: false
management:
  endpoints:
    web:
      exposure:
        include: '*'
